# ===== 运行时依赖（Render 部署用）=====

# FastAPI 框架
fastapi>=0.111.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0  # JWT 解析

# 向量数据库（新版包名已从 pinecone-client 改为 pinecone）
pinecone>=3.0.0

# 存储（Cloudflare R2 兼容 S3）
boto3>=1.34.0

# HTTP
httpx>=0.27.0
requests>=2.31.0

# 数值计算（嵌入向量处理）
numpy>=1.24.0

# 工具
python-dotenv>=1.0.0
